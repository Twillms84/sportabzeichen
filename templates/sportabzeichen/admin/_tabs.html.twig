<ul class="nav nav-tabs mb-3">
    <li class="nav-item">
        <a class="nav-link {{ activeTab == 'dashboard' ? 'active' }}"
           href="{{ path('sportabzeichen_admin_dashboard') }}">
            Übersicht
        </a>
    </li>

    <li class="nav-item">
        <a class="nav-link {{ activeTab == 'requirements' ? 'active' }}"
           href="{{ path('sportabzeichen_admin_requirements') }}">
            Anforderungen
        </a>
    </li>

    <li class="nav-item">
        <a class="nav-link {{ activeTab == 'participants' ? 'active' }}"
           href="{{ path('sportabzeichen_admin_participants') }}">
            Teilnehmer
        </a>
    </li>

    <li class="nav-item">
        <a class="nav-link {{ activeTab == 'exams' ? 'active' }}"
           href="{{ path('sportabzeichen_admin_exams') }}">
            Prüfungen
        </a>
    </li>
</ul>
